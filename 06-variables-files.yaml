- name: variables from files level
  hosts: localhost
  vars_files:
    C:\Users\User\DEVOPS\ansible-repo\variables.yaml
  tasks:
    - name:
      ansible.builtin.debug:
          msg: "Hi I am learning {{Course}}, trainer is {{Trainer}}, duration is {{Duration}}"
  